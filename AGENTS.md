# maksher 项目约定

- 应用名称是 `maksher`。不要把其他人名当作应用名称或包标识。
- 所有需要用户审核的文档必须使用中文，包括 `README.md`、`docs/plans/` 中的设计与实施计划、测试报告、评审报告和交接文档。代码标识符、命令、依赖名及外部资料标题可以保留原文，但说明文字与验收结论必须是中文。
- 在当前 `maksher` 工作区直接开发和提交应用代码。未经用户明确要求，不创建 Git worktree，也不把 maksher 的实现放在临时目录中。可在临时目录构建独立的第三方基线副本，但不得修改原始 `../velotype` 仓库。
- 项目使用 Superpowers 的设计、计划、实施与评审流程，不维护 `.devflow` 文档。
- 性能是首要目标。先验证 macOS 的真实启动、长文档打开、输入延迟和中文输入法；Windows 正式发布安排在下一版。
